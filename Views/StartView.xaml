<UserControl x:Class="RiskierWas.Views.StartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" xmlns:sys="clr-namespace:System;assembly=System.Runtime" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Riskier was!" FontSize="36" FontWeight="Bold" Foreground="White" Margin="8"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <StackPanel Width="420" Margin="8,8,24,8">
                <TextBlock Text="Teams" FontSize="20" Foreground="White" Margin="0,0,0,8"/>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,8">
                    <TextBlock Text="Anzahl:" Foreground="White" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox Width="80" SelectedItem="{Binding TeamCount, Mode=TwoWay}">
                        <sys:Int32>2</sys:Int32>
                        <sys:Int32>3</sys:Int32>
                        <sys:Int32>4</sys:Int32>
                    </ComboBox>
                </StackPanel>
                <ItemsControl ItemsSource="{Binding Teams}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=ItemsControl}, Path=TemplatedParent.Items.CurrentPosition}" Visibility="Collapsed"/>
                                <TextBlock Text="Name:" Foreground="White" Width="60"/>
                                <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                    <Button Content="Spiel starten" Command="{Binding StartGameCommand}" Padding="12,8" Margin="0,0,8,0"/>
                    <Button Content="Fragen bearbeiten" Command="{Binding OpenEditorCommand}" Padding="12,8"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Margin="8" VerticalAlignment="Top">
                <TextBlock Text="Fragen" FontSize="20" Foreground="White" Margin="0,0,0,8"/>
                <TextBlock Text="{Binding LoadStatus}" Foreground="LightGreen" Margin="0,0,0,8"/>
                <Button Content="Fragen laden…" Command="{Binding LoadQuestionsCommand}" Padding="12,8" Width="200"/>
                <TextBlock Text="(Standard: Data/questions.json neben der EXE)" Foreground="Gray" Margin="0,8,0,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
